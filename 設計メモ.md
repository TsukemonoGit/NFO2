# 設計メモ：フォローリストのソート

## 背景

- Nostrのフォローリストをソート機能付きで表示したい
- データはTanStack Queryの別々のクエリキャッシュに格納されている：
  - `["profile", pubkey]` → `Profile`
  - `["latestNote", pubkey]` → `Nostr.Event`
  - `["mutualStatus", pubkey]` → `MutualStatus`
- キャッシュから「同じpubkeyのデータを1セットに結合」してからソートする必要がある

## 決定事項

### 1. FollowerMap型の定義

コメントアウトされた`FollowerMap`型を有効化し、pubkeyごとの全データをまとめる型として定義する。

```typescript
export type FollowerEntry = {
  myPetname?: string;
  theirPetname?: string;
  latestCreated_at?: string;
  mutualStatus?: MutualStatus;
};

export type FollowerMap = Record<string, FollowerEntry>;
```

### 2. キャッシュ→FollowerMap変換関数の作成

TanStack Queryの別々のクエリ結果を結合し、1つの`FollowerMap`に変換する関数を作成する。

```typescript
function buildFollowerMap(
  pubkeys: string[],
  profiles: Record<string, Profile | undefined>,
  latestNotes: Record<string, Nostr.Event | undefined>,
  mutualStatuses: Record<string, MutualStatus | undefined>,
): FollowerMap;
```

### 3. ソート関数のインターフェース維持

既存の`sortFollowers`関数のシグネチャはそのまま維持する。

```typescript
export function sortFollowers(
  pubkeys: string[],
  map: FollowerMap,
  order: SortOrder,
): string[];
```

### 4. ソート順の管理

- ソート順の状態（`SortOrder`）はsvelte storeで管理
- ソート順の変更時はクエリ再取得は行わず、既存のキャッシュから再ソートする
- ソート済みリストの生成は表示側（computedまたはselect）で実行

### 5. 表示コンポーネントでのpipeline

```
キャッシュ取得 → 結合 → ソート → 表示

1. TanStack Queryから各クエリ結果を取得
2. buildFollowerMap()で結合
3. sortFollowers()でソート
4. ソート済みpubkeyリストをコンポーネントで描画
```

## 注意点

- **kind3の重複pubkey**
  - Nostrの仕様上、kind3のタグ配列に同じpubkeyが複数回入ることが可能
  - `FollowerMap` は `Record<string, FollowerEntry>` なので、重複があった場合は**最後に入ったタグの値が採用される**
  - ソートロジック自体には影響しないが、petnameの値は後勝ちのタグから取られる

## 未確定事項（要確認）

- [ ] `npub` の変換方法（実際の実装に依存）
- [ ] `petname` の保存先と取得方法（profileに含まれるか、別マップか）
- [ ] ソート順の永続化（localStorageに保存するか、セッションのみか）
